#include <stdio.h>
#define NUMVAL 5
int main(int argc, char*argv[]) {

	int numeri[NUMVAL]; /* int numeri[5]; */
	int i = 0;
	
	while(i < NUMVAL){
	scanf("%d", &numeri[i]);
	i++;
	}
	/* i vale 5 */
	
	i--;
	while(i >= 0){
	printf(%d, numeri[i]);
	i--;
	}
	
	printf("\n");
	
	return 0;
	
}
