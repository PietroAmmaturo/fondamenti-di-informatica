#include <stdio.h>
#include <string.h>
#define MAX_LEN 100

/**/
int main(int argc, char * argv[]){

	int arr[MAX_LEN], k, s, i, j, ris;
	
	i = 0;
	do{
	scanf("%d", &arr[i]);
	i++;
	}while (&arr[i] > 0 && i < MAX_LEN);
	 
	scanf("%d", &k);
	
	dim_arr = i;
	dim_arr1 = dim_arr-1;
	ris = 0;
	for(i = 0; i < dim_arr1 && !ris; i++)
		for(j = 0; j < dim_arr && !ris; j++){
			s = arr[i] + arr[j];
			if (s == K)
				ris = 1;
		}
		
		printf("%d", ris);
	return 0;
}

